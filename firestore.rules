rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Agenda única de Pamela e Itae (AMOR-0001 y códigos AMOR-####).
    match /households/{houseId} {
      allow read, write: if request.auth != null
        && houseId.matches('AMOR-[0-9]{4}');
    }
  }
}
